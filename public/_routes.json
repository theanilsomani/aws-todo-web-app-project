{
    "version": 1,
    "include": ["/*"],
    "exclude": [
       "/assets/*",    // Exclude your asset paths (adjust if different)
       "/favicon.ico", // Add other specific files to exclude if needed
       "/vite.svg"     // Example
      ],
    "routes": [
      {
        "src": "/.*", // Match any route
        "dest": "/index.html",
        "status": 200
      }
    ]
  }